# Database Credentials
db.driverName = com.mysql.cj.jdbc.Driver
# Use the MySQL container name as host when running in Docker
db.connectionString = jdbc:mysql://shopping-cart-mysql:3306/shopping-cart
db.username = appuser
db.password = apppassword

# Mailer Credentials (override via environment in production)
mailer.email=your_email
mailer.password=your_app_password_generated_from_email